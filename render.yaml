services:
  - type: web
    name: yoyo-ai-partner
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn api.chat:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9