# AI-Дос - Интеллектуальный помощник знаний 🚀

![AI-Дос Помощник](./public/dos-avatar.png)

Это интеллектуальный помощник знаний, специально разработанный для детей, по имени «AI-Дос». Он не только может отвечать на различные вопросы детей эмоциональным голосом, но и «видеть» изображения, которыми делятся дети (например, их рисунки), и давать тёплые поощрения и похвалу.

## ✨ Основные функции

*   **Эмоциональный синтез речи**: Ответы AI-Дос разбиваются на несколько речевых фрагментов с разными эмоциями (например, радость, утешение, воодушевление) и синтезируются в естественную, тёплую речь.
*   **Мультимодальное понимание**: Дети могут фотографировать или загружать изображения, AI-Дос способен понимать содержание изображений и давать соответствующие ответы, особенно хорошо подходит для похвалы детских рисунков.
*   **Раздельная архитектура фронтенда и бэкенда**: Фронтенд представляет собой чистый статический сайт, бэкенд - отдельный веб-сервис. Это современная, масштабируемая архитектура веб-приложения.
*   **Дружественное к детям взаимодействие**: Специально разработанные системные промпты (System Prompt) делают ответы AI-Дос всегда полными воображения, поддержки и позитивной энергии.
*   **Голосовой ввод**: Поддержка голосового ввода через микрофон, что удобно для детей, которым трудно печатать.

## 🛠️ Используемые технологии

*   **Бэкенд (Backend)**:
    *   **Фреймворк**: Flask
    *   **Языковая модель**: Google Gemini 1.5 Flash (мультимодальная и JSON вывод)
    *   **Синтез речи (TTS)**: Azure Cognitive Services Speech
    *   **Сервер**: Gunicorn
    *   **Деплой**: **Render (Web Service)**

*   **Фронтенд (Frontend)**:
    *   HTML5 / CSS3 / JavaScript
    *   Web Speech API (распознавание речи)
    *   Fetch API
    *   **Деплой**: **Render (Static Site)**

## 🚀 Как запустить локально

Для имитации реальной среды деплоя мы запустим backend и frontend серверы отдельно.

1.  **Клонируйте проект**
    ```bash
    git clone https://github.com/ganbing0630/yoyo-ai-partner.git
    cd yoyo-ai-partner
    ```

2.  **Создайте виртуальное окружение и установите зависимости**
    ```bash
    # Создание виртуального окружения
    python -m venv venv
    # Активация виртуального окружения (macOS/Linux)
    source venv/bin/activate
    # Активация виртуального окружения (Windows)
    # .\venv\Scripts\activate
    
    # Установка всех зависимостей
    pip install -r requirements.txt
    ```

3.  **Настройте переменные окружения**
    *   Создайте файл `.env` в корневой директории проекта.
    *   Заполните необходимые API ключи:
        ```
        GEMINI_API_KEY="ВАШ_КЛЮЧ_GEMINI"
        SPEECH_KEY="ВАШ_КЛЮЧ_AZURE_SPEECH"
        SPEECH_REGION="ВАШ_РЕГИОН_AZURE_SPEECH"
        ```

4.  **Запустите backend сервер (первый терминал)**
    *   В корневой директории проекта выполните следующую команду для запуска разработческого сервера Flask:
    ```bash
    flask --app api/chat run
    ```
    *   Вы должны увидеть, что сервер запущен на `http://127.0.0.1:5000`. **Держите этот терминал открытым.**

5.  **Запустите frontend сервер (второй терминал)**
    *   Откройте **новое окно терминала**.
    *   Перейдите в папку `public` и запустите простой HTTP сервер:
    ```bash
    cd public
    python -m http.server 8080
    ```
    *   Ваш фронтенд теперь запущен на `http://127.0.0.1:8080`.

6.  **Измените путь API и протестируйте**
    *   Чтобы фронтенд мог найти backend, работающий на порту `5000`, **временно измените** `API_URL` в файле `public/script.js`:
        ```javascript
        // const API_URL = "/api/chat"; // сначала закомментируйте эту строку
        const API_URL = "http://127.0.0.1:5000/api/chat"; // временно используйте эту строку
        ```
    *   Теперь откройте в браузере **`http://127.0.0.1:8080`** и вы можете полностью протестировать взаимодействие с «AI-Дос»!
    *   **Важное примечание**: Когда вы будете отправлять изменения на GitHub для деплоя, не забудьте **вернуть** `API_URL` обратно на относительный путь `"/api/chat"`.

## Деплой на Render

Архитектура этого проекта идеально подходит для раздельного деплоя фронтенда и бэкенда на Render. Вам нужно создать два сервиса:

#### 1. Бэкенд: Web Service

*   **Платформа**: Render
*   **Тип сервиса**: Web Service
*   **Среда**: Python
*   **Команда сборки (`Build Command`)**: `pip install -r requirements.txt`
*   **Команда запуска (`Start Command`)**: `gunicorn 'api.chat:app'`
*   **Переменные окружения**: В панели управления Render установите ваши `GEMINI_API_KEY`, `SPEECH_KEY`, `SPEECH_REGION`.

#### 2. Фронтенд: Static Site

*   **Платформа**: Render
*   **Тип сервиса**: Static Site
*   **Директория публикации (`Publish Directory`)**: `public`
*   **Правила перезаписи/перенаправления (`Redirects/Rewrites`)**:
    *   **Action**: `Rewrite`
    *   **Source**: `/api/:path*`
    *   **Destination**: `https://<адрес_вашего_бэкенд_сервиса>.onrender.com/api/:path*`

---
*Этот проект создан как пример разработки AI-приложения и современного деплоя.*
